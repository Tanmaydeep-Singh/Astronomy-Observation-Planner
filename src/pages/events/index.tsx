import axios from 'axios'
import React from 'react'

function index() {

  const starChart = async() => {
  }
  return (
    <div>UPCOMMING EVENTS LIST
      <button onClick={ () => { starChart()}}>BUTTON</button>
    </div>
  )
}

export default index
